name: core_router
description: "路由能力：RouteDefinition、go_router 装配与 AppNavigator。"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.13.3

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  go_router: ^18.0.1
  core_module: ^0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # 测试里直接构造 ServiceLocator 来 boot 注册表。
  core_di: ^0.0.1

flutter:
