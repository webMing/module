name: core_observability
description: "可观测性门面：日志/崩溃/埋点/性能/Tracing 统一入口。"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.13.3

resolution: workspace

dependencies:
  core_logger: ^0.0.1
  core_error: ^0.0.1
  meta: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
