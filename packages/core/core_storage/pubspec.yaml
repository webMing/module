name: core_storage
description: "持久化抽象：KeyValueStore 接口与内存实现。"
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ^3.13.3

resolution: workspace

dependencies:
  core_error: ^0.0.1
  core_model: ^0.0.1
  meta: ^1.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
